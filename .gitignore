# Local secrets and runtime config
config.yaml
.env

# Rendered images / runtime data
/images/
/data/

# Build output
/bus-trmnl
/out/

# Claude Code local settings (personal, not committed)
.claude/settings.local.json
